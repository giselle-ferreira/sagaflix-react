import styled from 'styled-components';

export const Container = styled.div`
    img {
        width: auto;
        height: 300px;
        object-fit: cover;
        border-radius: .6em;
        margin: 2em 3em;
    }
`